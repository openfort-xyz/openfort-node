/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * Openfort API
 * Complete Openfort API references and guides can be found at: https://www.openfort.io/docs
 * OpenAPI spec version: 1.0.0
 */
import type {
  CheckoutFundingOnrampSession200,
  CheckoutFundingOnrampSessionBody,
  CreateFundingSessionRequest,
  CreateOnrampAuthIntentBody,
  CreateOnrampVerificationBody,
  FundingChainsResponse,
  FundingConfigResponse,
  FundingSessionResponse,
  GetFundingOnrampMethodsParams,
  GetFundingSessionMethodsParams,
  GetFundingSessionParams,
  GetOnrampIdentityParams,
  GetOnrampLimitsParams,
  ListChainsParams,
  OnrampAuthIntentResponse,
  OnrampAuthTokenExchangeResponse,
  OnrampIdentityResponse,
  OnrampLimitUpgradeResponse,
  OnrampLimitsResponse,
  OnrampMethodsResponse,
  OnrampQuoteResponse,
  OnrampVerificationResponse,
  OnrampVerificationSubmitResponse,
  PayLinkRequest,
  PayLinkResponse,
  SessionQuoteRequest,
  SetPaymentMethodRequest,
  StartOnrampLimitUpgradeBody,
  SubmitOnrampVerificationBody,
  UpdateFundingConfigRequest
} from '../openfortAPI.schemas';

import { openfortApiClient } from '../../openfortApiClient';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  /**
 * Starts an OTP verification for the buyer's phone or email — the identity
 * step of native wallet pay (Apple/Google Pay). The code is sent to the
 * destination; submit it via the submit endpoint, then attach the returned
 * ids to the wallet-pay commit as `smsVerificationId`/`emailVerificationId`.
 * @summary Start a wallet-pay OTP verification
 */
export const createOnrampVerification = (
    createOnrampVerificationBody: CreateOnrampVerificationBody,
 options?: SecondParameter<typeof openfortApiClient<OnrampVerificationResponse>>,) => {
      return openfortApiClient<OnrampVerificationResponse>(
      {url: `/v2/funding/onramp/verifications`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createOnrampVerificationBody
    },
      options);
    }
  /**
 * Submits the OTP the buyer received for a verification. On success the
 * returned id rides on the wallet-pay commit.
 * @summary Submit a wallet-pay OTP code
 */
export const submitOnrampVerification = (
    verificationId: string,
    submitOnrampVerificationBody: SubmitOnrampVerificationBody,
 options?: SecondParameter<typeof openfortApiClient<OnrampVerificationSubmitResponse>>,) => {
      return openfortApiClient<OnrampVerificationSubmitResponse>(
      {url: `/v2/funding/onramp/verifications/${verificationId}/submit`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: submitOnrampVerificationBody
    },
      options);
    }
  /**
 * Mints an auth intent for the buyer's email — step 1 of the embedded
 * (headless element) flow. The client renders the provider's auth element
 * with the returned id; after the buyer completes it, exchange the intent
 * for its server-side token via the tokens endpoint.
 * @summary Create an embedded-flow auth intent
 */
export const createOnrampAuthIntent = (
    createOnrampAuthIntentBody: CreateOnrampAuthIntentBody,
 options?: SecondParameter<typeof openfortApiClient<OnrampAuthIntentResponse>>,) => {
      return openfortApiClient<OnrampAuthIntentResponse>(
      {url: `/v2/funding/onramp/auth_intents`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createOnrampAuthIntentBody
    },
      options);
    }
  /**
 * Exchanges a completed auth intent for its provider token — step 3 of the
 * embedded flow, after the buyer finishes the auth element. The token stays
 * server-side; committing the session and checking out both look it up by
 * the intent id.
 * @summary Exchange an embedded-flow auth intent for its token
 */
export const exchangeOnrampAuthToken = (
    intentId: string,
 options?: SecondParameter<typeof openfortApiClient<OnrampAuthTokenExchangeResponse>>,) => {
      return openfortApiClient<OnrampAuthTokenExchangeResponse>(
      {url: `/v2/funding/onramp/auth_intents/${intentId}/tokens`, method: 'POST'
    },
      options);
    }
  /**
 * Reads the buyer's identity state at the provider — which regulatory
 * region they fall under, the verification tier they have reached, and
 * which identity steps they have already satisfied.
 *
 * The embedded flow needs this to prompt for exactly what is outstanding:
 * without it a client can only infer the remaining steps, and re-asks for
 * declarations the buyer already made.
 * @summary Read the buyer's onramp identity state
 */
export const getOnrampIdentity = (
    params: GetOnrampIdentityParams,
 options?: SecondParameter<typeof openfortApiClient<OnrampIdentityResponse>>,) => {
      return openfortApiClient<OnrampIdentityResponse>(
      {url: `/v2/funding/onramp/identity`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Reads the buyer's current transaction limits. Clients compare the
 * intended purchase against these to offer a verification step-up instead
 * of letting the commit fail.
 *
 * Monetary values are in CENTS.
 * @summary Read the buyer's onramp transaction limits
 */
export const getOnrampLimits = (
    params?: GetOnrampLimitsParams,
 options?: SecondParameter<typeof openfortApiClient<OnrampLimitsResponse>>,) => {
      return openfortApiClient<OnrampLimitsResponse>(
      {url: `/v2/funding/onramp/limits`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Starts the provider-hosted identity form that raises a wallet-pay buyer's
 * limits, returning the URL to embed.
 *
 * The form is hosted by the provider because it collects a partial SSN —
 * that data never passes through Openfort. The URL is single-use and
 * short-lived, so request a fresh one per attempt rather than caching it.
 * @summary Start a wallet-pay limit upgrade
 */
export const startOnrampLimitUpgrade = (
    startOnrampLimitUpgradeBody: StartOnrampLimitUpgradeBody,
 options?: SecondParameter<typeof openfortApiClient<OnrampLimitUpgradeResponse>>,) => {
      return openfortApiClient<OnrampLimitUpgradeResponse>(
      {url: `/v2/funding/onramp/limits/upgrade`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: startOnrampLimitUpgradeBody
    },
      options);
    }
  /**
 * Resolves the fiat onramp methods available for a destination and the buyer's
 * region. Openfort auto-selects the provider per method (by country, asset, and
 * whether the project has native wallet-pay unlocked) — the provider is never
 * shown to or chosen by the end user; only the resolved method rows are returned.
 *
 * The country is taken from the `country` query param, else the request IP
 * (`cf-ipcountry` behind Cloudflare). `methods` is an optional comma-separated
 * allowlist in display order; omit to return all web2 methods. `angles` is an
 * optional comma-separated list of integration angles the client can execute
 * (e.g. "popup" from React Native); omit for no restriction.
 * @summary Resolve onramp methods
 */
export const getFundingOnrampMethods = (
    params: GetFundingOnrampMethodsParams,
 options?: SecondParameter<typeof openfortApiClient<OnrampMethodsResponse>>,) => {
      return openfortApiClient<OnrampMethodsResponse>(
      {url: `/v2/funding/onramp/methods`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Lists the source chains and currencies the funding rail can route from.
 *
 * A live passthrough of the rail's dictionary (Relay), narrowed client-side
 * to a curated subset. Public — it exposes no tenant data, so the source
 * picker can load it before the user authenticates.
 * @summary List funding source chains.
 */
export const listChains = (
    params?: ListChainsParams,
 options?: SecondParameter<typeof openfortApiClient<FundingChainsResponse>>,) => {
      return openfortApiClient<FundingChainsResponse>(
      {url: `/v2/funding/chains`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Creates a funding session for a destination.
 *
 * A session is one cross-chain deposit attempt: pick a destination chain,
 * token, and wallet, then set a payment method to mint a deposit address.
 * The response includes a `clientSecret` used to advance and read the session.
 * @summary Create a funding session.
 */
export const createFundingSession = (
    createFundingSessionRequest: CreateFundingSessionRequest,
 options?: SecondParameter<typeof openfortApiClient<FundingSessionResponse>>,) => {
      return openfortApiClient<FundingSessionResponse>(
      {url: `/v2/funding/sessions`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createFundingSessionRequest
    },
      options);
    }
  /**
 * Sets the (single) payment method on a session.
 *
 * Commits the source route the user will send from and mints the Relay
 * deposit address. Requires the session `clientSecret`.
 * @summary Set a session's payment method.
 */
export const setPaymentMethod = (
    sessionId: string,
    setPaymentMethodRequest: SetPaymentMethodRequest,
 options?: SecondParameter<typeof openfortApiClient<FundingSessionResponse>>,) => {
      return openfortApiClient<FundingSessionResponse>(
      {url: `/v2/funding/sessions/${sessionId}/payment_methods`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: setPaymentMethodRequest
    },
      options);
    }
  /**
 * Embedded (headless) checkout: confirms the committed headless onramp
 * session with the buyer's mandate acceptance (this request's ip + user
 * agent) and returns the provider client secret for the client's embedded
 * checkout element. One-shot — the secret is never stored.
 * @summary Check out an embedded onramp session
 */
export const checkoutFundingOnrampSession = (
    sessionId: string,
    checkoutFundingOnrampSessionBody: CheckoutFundingOnrampSessionBody,
 options?: SecondParameter<typeof openfortApiClient<CheckoutFundingOnrampSession200>>,) => {
      return openfortApiClient<CheckoutFundingOnrampSession200>(
      {url: `/v2/funding/sessions/${sessionId}/onramp_checkout`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: checkoutFundingOnrampSessionBody
    },
      options);
    }
  /**
 * Resolves the fiat onramp methods available for a session's destination and
 * the buyer's region. Openfort auto-selects the provider per method — the
 * client renders the rows and commits one via `setPaymentMethod` with
 * `{ type: "onramp", method }`. Requires the session `clientSecret`.
 * @summary Resolve a session's available funding methods.
 */
export const getFundingSessionMethods = (
    sessionId: string,
    params?: GetFundingSessionMethodsParams,
 options?: SecondParameter<typeof openfortApiClient<OnrampMethodsResponse>>,) => {
      return openfortApiClient<OnrampMethodsResponse>(
      {url: `/v2/funding/sessions/${sessionId}/methods`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Prices a fiat onramp route for a session before committing it. The provider
 * is resolved exactly as `setPaymentMethod` would resolve it, so the quote
 * matches the checkout the user will get. Requires the session `clientSecret`.
 * @summary Quote a session's fiat funding route.
 */
export const quoteFundingSession = (
    sessionId: string,
    sessionQuoteRequest: SessionQuoteRequest,
 options?: SecondParameter<typeof openfortApiClient<OnrampQuoteResponse>>,) => {
      return openfortApiClient<OnrampQuoteResponse>(
      {url: `/v2/funding/sessions/${sessionId}/quotes`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: sessionQuoteRequest
    },
      options);
    }
  /**
 * Retrieves a funding session, refreshing its status from the rail.
 *
 * Requires the session `clientSecret` (query parameter).
 * @summary Get a funding session.
 */
export const getFundingSession = (
    sessionId: string,
    params?: GetFundingSessionParams,
 options?: SecondParameter<typeof openfortApiClient<FundingSessionResponse>>,) => {
      return openfortApiClient<FundingSessionResponse>(
      {url: `/v2/funding/sessions/${sessionId}`, method: 'GET',
        params
    },
      options);
    }
  /**
 * Builds a prefilled centralized-exchange (cex) transfer link for a session.
 *
 * Returns a hosted exchange transfer URL: the user signs into their own
 * exchange account and the exchange delivers the asset to the session's
 * wallet, handling any bridging from the balance they hold. Destination
 * address and chain are derived from the session, not the client. The active
 * cex provider is resolved server-side. Requires the session `clientSecret`.
 * @summary Build a centralized-exchange transfer link for a session.
 */
export const createPayLink = (
    payLinkRequest: PayLinkRequest,
 options?: SecondParameter<typeof openfortApiClient<PayLinkResponse>>,) => {
      return openfortApiClient<PayLinkResponse>(
      {url: `/v2/funding/pay_link`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: payLinkRequest
    },
      options);
    }
  /**
 * Returns the project's funding configuration (the dashboard "Funding" section),
 * or the defaults when nothing has been saved yet.
 * @summary Get funding config
 */
export const getFundingConfig = (

 options?: SecondParameter<typeof openfortApiClient<FundingConfigResponse>>,) => {
      return openfortApiClient<FundingConfigResponse>(
      {url: `/v2/funding/config`, method: 'GET'
    },
      options);
    }
  /**
 * Creates or replaces the project's funding configuration.
 * @summary Update funding config
 */
export const updateFundingConfig = (
    updateFundingConfigRequest: UpdateFundingConfigRequest,
 options?: SecondParameter<typeof openfortApiClient<FundingConfigResponse>>,) => {
      return openfortApiClient<FundingConfigResponse>(
      {url: `/v2/funding/config`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: updateFundingConfigRequest
    },
      options);
    }
  export type CreateOnrampVerificationResult = NonNullable<Awaited<ReturnType<typeof createOnrampVerification>>>
export type SubmitOnrampVerificationResult = NonNullable<Awaited<ReturnType<typeof submitOnrampVerification>>>
export type CreateOnrampAuthIntentResult = NonNullable<Awaited<ReturnType<typeof createOnrampAuthIntent>>>
export type ExchangeOnrampAuthTokenResult = NonNullable<Awaited<ReturnType<typeof exchangeOnrampAuthToken>>>
export type GetOnrampIdentityResult = NonNullable<Awaited<ReturnType<typeof getOnrampIdentity>>>
export type GetOnrampLimitsResult = NonNullable<Awaited<ReturnType<typeof getOnrampLimits>>>
export type StartOnrampLimitUpgradeResult = NonNullable<Awaited<ReturnType<typeof startOnrampLimitUpgrade>>>
export type GetFundingOnrampMethodsResult = NonNullable<Awaited<ReturnType<typeof getFundingOnrampMethods>>>
export type ListChainsResult = NonNullable<Awaited<ReturnType<typeof listChains>>>
export type CreateFundingSessionResult = NonNullable<Awaited<ReturnType<typeof createFundingSession>>>
export type SetPaymentMethodResult = NonNullable<Awaited<ReturnType<typeof setPaymentMethod>>>
export type CheckoutFundingOnrampSessionResult = NonNullable<Awaited<ReturnType<typeof checkoutFundingOnrampSession>>>
export type GetFundingSessionMethodsResult = NonNullable<Awaited<ReturnType<typeof getFundingSessionMethods>>>
export type QuoteFundingSessionResult = NonNullable<Awaited<ReturnType<typeof quoteFundingSession>>>
export type GetFundingSessionResult = NonNullable<Awaited<ReturnType<typeof getFundingSession>>>
export type CreatePayLinkResult = NonNullable<Awaited<ReturnType<typeof createPayLink>>>
export type GetFundingConfigResult = NonNullable<Awaited<ReturnType<typeof getFundingConfig>>>
export type UpdateFundingConfigResult = NonNullable<Awaited<ReturnType<typeof updateFundingConfig>>>
